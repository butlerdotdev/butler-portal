apiVersion: v1
kind: Service
metadata:
  name: {{ include "butler-portal.fullname" . }}
  labels:
    {{- include "butler-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "butler-portal.selectorLabels" . | nindent 4 }}
