Butler Portal {{ .Chart.AppVersion }} has been installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Service-account credentials for butler-server are read from the Secret
"{{ .Values.butlerAuth.existingSecret }}" in this namespace, with keys:
  - {{ .Values.butlerAuth.usernameKey | default "username" }}
  - {{ .Values.butlerAuth.passwordKey | default "password" }}

If this Secret does not exist yet, the portal pod will not start. Create it with:

  kubectl create secret generic {{ .Values.butlerAuth.existingSecret }} \
    --namespace {{ .Release.Namespace }} \
    --from-literal=username='<service-account-username>' \
    --from-literal=password='<service-account-password>'

Portal base URL: {{ .Values.appConfig.baseUrl }}

To check pod status:
  kubectl get pods --namespace {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "butler-portal.name" . }}
